{
  "name": "queue-client",
  "version": "1.0.0",
  "description": "An npm module for findTopN using priority queues",
  "main": "index.ts",
  "files": [
    "lib/**/*"
  ],
  "baseUrl": "./src",
  "exports": {
    "queue-client": "./index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "eslint --fix src/**/*.{js,jsx,ts,tsx}",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
